<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid d-flex align-items-center">

    <!-- LEFT: Brand + Explore -->
    <div class="d-flex align-items-center">
      <a class="navbar-brand me-2" href="/listings">
        <i class="fa-solid fa-hotel"></i>
      </a>

      <div class="collapse navbar-collapse show-on-desktop" id="navbarNavLeft">
        <a class="nav-link" href="/listings">
          <i class="bi bi-compass"></i>
          <span class="fw-semibold">Explore</span>
        </a>
      </div>
    </div>

    <!-- 🔥 CENTER: SEARCH (ALWAYS VISIBLE) -->
    <div class="search-container mx-auto">
      <form class="search-form" role="search">
        <input
          class="search-input"
          type="search"
          placeholder="Search destinations, properties..."
        />
        <button class="search-btn" type="submit">Search</button>
      </form>
    </div>

    <!-- RIGHT: TOGGLER + AUTH -->
    <button class="navbar-toggler ms-2" type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavRight">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNavRight">
      <a class="nav-link" href="/listings/new">
        <i class="fa-solid fa-house me-1"></i> Become a Host
      </a>

      <% if(!curruser) { %>
        <a class="nav-link" href="/signup">
          <i class="bi bi-person-plus me-1"></i><b>Sign Up</b>
        </a>
        <a class="nav-link" href="/login">
          <i class="bi bi-box-arrow-in-right me-1"></i><b>Log In</b>
        </a>
      <% } %>

      <% if(curruser) { %>
        <a class="nav-link" href="/logout">Log-out</a>
      <% } %>
    </div>

  </div>
</nav>
